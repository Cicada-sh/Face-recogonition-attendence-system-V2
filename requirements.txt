# Core dependencies
opencv-python==4.8.1.78
numpy==1.26.4
flask==2.3.3
flask-cors==4.0.0
pillow==10.0.1

# Deep learning framework for face recognition
torch==2.0.1
torchvision==0.15.2
facenet-pytorch==2.5.3

# Note: face-recognition package removed due to dlib compilation issues on Windows
# The application uses facenet-pytorch for face recognition instead
